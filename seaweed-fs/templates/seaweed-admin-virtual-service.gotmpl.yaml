apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: seaweed-admin-virtual-service
spec:
  hosts:
    - seaweed.karafra.net
  gateways:
    - infra-network/public-ingress-gateway
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: seaweedfs-admin.infra-storage.svc.cluster.local
            port:
              number: 23646
